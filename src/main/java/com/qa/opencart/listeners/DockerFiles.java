package com.qa.opencart.listeners;

public class DockerFiles {
	
	//download docker desktop
	
	//open command prompt
	
	//docker --version
	
	//docker images - check images
	//docker ps -a - check containers
	
	//docker-compose up -d - runs the yml file and uploads images and containers
	
	//docker-compose up -d --scale chrome=4 // total 4 chrome browsers will be available
	
	//docker-compose up -d --scale chrome=4 --scale firefox=3 --scale edge=2

}
